ktor {
    deployment {
        port = 8080
        port = ${?PORT}
    }
    application {
        modules = [ no.nav.helse.PleiepengesoknadProsesseringKt.pleiepengesoknadProsessering ]
    }
}
nav {
    rest_api {
        authorized_systems = "srvpleiepengesokna",
        authorized_systems = ${?REST_API_AUTHORIZED_SYSTEMS}
    }
    pleiepenger_dokument_base_url = "http://localhost:1234"
    pleiepenger_dokument_base_url = ${?PLEIEPENGER_DOKUMENT_BASE_URL}
    opprett_sak_url = "http://localhost:4568"
    opprett_sak_url = ${?OPPRETT_SAK_URL}
    opprett_oppgave_url = "http://localhost:4567"
    opprett_oppgave_url = ${?OPPRETT_OPPGAVE_URL}
    opprett_journal_post_url = "http://localhost:4566"
    opprett_journal_post_url = ${?OPPRETT_JOURNAL_POST_ID}
    aktoer_register_base_url = "http://localhost:4565"
    aktoer_register_base_url = ${?AKTOER_REGISTER_BASE_URL}
    authorization {
        token_url = "http://localhost:8083/token"
        token_url = ${?TOKEN_URL}
        jwks_url = "http://localhost:8084/jwk-set"
        jwks_url = ${?JWKS_URL}
        issuer = "foo"
        issuer = ${?ISSUER}
        service_account {
            client_id = "srvpps-prosessering"
            client_id = ${?SERVICE_ACCOUNT_CLIENT_ID}
            client_secret = "bar"
            client_secret = ${?SERVICE_ACCOUNT_CLIENT_SECRET}
            scopes = "openid"
            scopes = ${?SERVICE_ACCOUNT_SCOPES}
        }
    }
}